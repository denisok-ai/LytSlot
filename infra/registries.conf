# Podman: разрешить короткие имена образов (lytslot-*:latest).
# Скопировать: cp infra/registries.conf ~/.config/containers/registries.conf
# (mkdir -p ~/.config/containers если папки нет)

unqualified-search-registries = ["docker.io"]
